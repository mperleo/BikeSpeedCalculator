import { TranslationsPipe } from './translations.pipe';

describe('TranslationsPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslationsPipe();
    expect(pipe).toBeTruthy();
  });
});
